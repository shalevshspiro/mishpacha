function scrollToDonate() {  const donateSection = document.getElementById("donate");  if (!donateSection) return;  const targetOffset = donateSection.getBoundingClientRect().top + window.pageYOffset;  const startPosition = window.pageYOffset;  const distance = targetOffset - startPosition;  const duration = 800;  let start = null;  function step(timestamp) {    if (!start) start = timestamp;    const progress = timestamp - start;    const easeInOut = (t) =>      t < 0.5 ? 2 * t * t : -1 + (4 - 2 * t) * t;    window.scrollTo(0, startPosition + distance * easeInOut(progress / duration));    if (progress < duration) {      window.requestAnimationFrame(step);    }  }  window.requestAnimationFrame(step);}document.addEventListener("DOMContentLoaded", () => {  const logoContainer = document.getElementById("logo-container");  const logo = document.createElement("img");  logo.src = "img1/logo.png";  logo.alt = "לוגו העמותה";  logo.className = "logo";  logoContainer.appendChild(logo);  const galleryImages = ["img1/vision1.png", "img1/vision2.png", "img1/vision3.png"];  const galleryContainer = document.getElementById("gallery-container");  galleryImages.forEach(src => {    const img = document.createElement("img");    img.src = src;    img.alt = "החזון שלנו בתמונות";    galleryContainer.appendChild(img);  });  const videoContainer = document.getElementById("video-container");  const iframe = document.createElement("iframe");  iframe.src = "https://www.youtube.com/embed/6S4ANuwKKr0";  iframe.allowFullscreen = true;  videoContainer.appendChild(iframe);  const form = document.querySelector("form");  form.addEventListener("submit", () => {    setTimeout(() => {      alert("הפרטים התקבלו. נהיה בקשר. תודה על התמיכה!");    }, 100);  });});